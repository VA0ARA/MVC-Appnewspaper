@model List<News.Models.Category>
@*<partial name="_Notification" />*@
<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3 ">
        <div class="row">
            <div class=" col-12 text-center">
                <h2 class="text-white py-2"> لیست گروه های خبری</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6 text-end">
        </div>
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>
                        نام گروه خبری
                    </th>
                    <th>
                        ظرفیت
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <th>
                            @item.Name
                        </th>
                        <th>
                            @item.capacity
                        </th>
                        <th>
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="Category" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary mx-2">
                                    <i class="bi bi-pencil-square"></i>ویرایش
                                </a>
                                <a asp-controller="Category" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash3"></i>حذف
                                </a>
                            </div>

                        </th>
                    </tr>
                }
            </tbody>
        </table>
        <div class="col-6 text-end">
            <a asp-controller="Admin" asp-action="AdminProfile" class="btn btn-primary">
                بازگشت به پروفایل
            </a>
        </div>
    </div>
</div>

@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}



